
import React from "react";
import ReactDOM from "react-dom/client";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <div style={{ fontSize: "24px", padding: "20px" }}>
    Chào mừng Quang đến với App học từ vựng!
  </div>
);
